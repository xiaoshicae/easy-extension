/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */

/**
 * 布局组件
 */
import CodeHighlight from './CodeHighlight';
import Footer from './Footer';
import { Document, Github, SelectLang } from './HeaderContent';

export { CodeHighlight, Footer, Document, SelectLang, Github };
